#!/bin/sh
mount -t proc proc /proc
mount -t sysfs sysfs /sys
mount -t devtmpfs devtmpfs /dev 2>/dev/null || true

echo "Initramfs: /init started"
echo "cmdline: $(cat /proc/cmdline)"

# keep it simple: drop to shell for now
exec /bin/sh